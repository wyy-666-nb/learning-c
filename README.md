# C 语言学习记录

从零开始学 C 语言的练习仓库。2026 年 9 月 11 日开始，尽量保持每天都有提交。

## 这个仓库是干什么的

记录我从零学 C 语言的完整过程：**每天写的代码、踩过的坑、每一次提交**。

目标是为计算机学科基础（数据结构、408）以及后续的人工智能方向学习打好基础。

## 目录结构

| 目录              | 内容                 | 状态     |
| --------------- | ------------------ | ------ |
| `syntax-drill/` | **语法练习**：一个语法点一个文件 | 🚧 进行中 |
| `01-basics/`    | **完整小程序**：分支、循环    | 🚧 进行中 |
| `02-functions/` | 完整小程序：函数、数组        | ⬜ 未开始  |
| `03-pointers/`  | 完整小程序：指针           | ⬜ 未开始  |
| `04-structs/`   | 完整小程序：结构体、动态内存     | ⬜ 未开始  |

## 两类文件的区别

### `syntax-drill/` —— 跟课练习

学到一个新语法，就复制 `00-模板.c`，重命名成 `序号-语法点.c` 来写。

每个文件头部记录：**语法点 / 来源 / 它解决什么问题 / 语法格式 / 关键注意点 / 和相似语法的区别**。

目的是加深印象，以后复习时按序号就能找到。

### `01-basics/` 等目录 —— 独立完成的小程序

学完一章后，**关掉视频、自己独立写**的完整程序。这类才是我真正的收获。

## 学习进度

### 分支

- [x] `if` / `else if` / `else`
- [x] 关系操作符 / 条件操作符 / 逻辑操作符
- [x] `switch` 与 `case` 穿透

### 循环

- [x] `while`
- [x] `for`
- [x] `do-while`
- [x] `break` / `continue`
- [x] 循环嵌套
- [x] `goto`

### 接下来

- [ ] 数组
- [ ] 函数
- [ ] 指针
- [ ] 结构体
- [ ] 动态内存

## 学习日志

| 日期         | 学了什么                                   | 卡在哪              |
| ---------- | -------------------------------------- | ---------------- |
| 2026-09-11 | `if` 语句                                | 嵌套 `if` 的括号容易漏   |
| 2026-09-13 | `switch` / `while`                     |                  |
| 2026-09-14 | `for` 语句                               |                  |
| 2026-09-15 | `do-while` / `break`-`continue` / 循环嵌套 | 嵌套循环的边界容易写错      |
| 2026-09-16 | `goto` / 随机数生成                         | 中文注释导致编译错误（编码问题） |
| 2026-09-28 | 分支小程序                                  | 逻辑操作符运用不熟练       |

## 踩过的坑

记录下来，避免以后重复踩。

### 1. 中文注释导致编译报错（最坑的一个）

**症状**：代码明明没写错，却报"未声明的标识符""语法错误"。

**原因**：源文件是 UTF-8 但没有 BOM，MSVC 会按 GBK 读取。中文注释的最后一个字节会把后面的换行符"吃掉"，导致下一行代码被并进注释里，大括号结构随之错乱。

**解决**：所有 `.c` 文件保存为 **UTF-8 带签名（BOM）**。

### 2. VS 项目的命名

- 项目名称**不要带 `.c` 后缀**——否则源文件会被命名成 `xxx.c.c`
- 项目名称和文件夹名**不要有空格**——否则生成中间目录时名字会被截断

### 3. Windows 11 智能应用控制

它会拦截所有自己编译的、没有数字签名的 `.exe`。学编程必须关掉它，否则程序编译成功却运行不了。

### 4. 批处理文件（`.cmd` / `.bat`）的编码

- 编码要用 **GBK**（或纯英文内容）
- 换行必须是 **CRLF**
- 文件名尽量用 **ASCII**

## 开发环境

- **IDE**：Visual Studio 2026 Community（MSVC 14.51）
- **项目类型**：空项目（C++ 模板），但源文件后缀用 `.c`，这样编译器按 C 语言处理
- **源文件编码**：UTF-8 带签名（BOM）

## 常用命令

```powershell
# 提交并推送
git add .
git commit -m "feat: 添加 xxx 练习"
git push

# 看状态（出现 [ahead] 说明还有提交没推上去）
git status -sb

# 看提交历史
git log --oneline
```

## 提交信息规范

| 前缀          | 用途         |
| ----------- | ---------- |
| `feat:`     | 新增程序或练习    |
| `fix:`      | 修复 bug     |
| `docs:`     | 只改文档       |
| `refactor:` | 重构代码（功能不变） |
| `chore:`    | 杂项，比如配置    |
